apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-f3b5"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-f3b5"
  template:
    metadata:
      labels:
        app: "recogaks-f3b5"
    spec:
      containers:
        - name: "recogaks-f3b5"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 3478