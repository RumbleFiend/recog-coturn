apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-coturn"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-coturn"
  template:
    metadata:
      labels:
        app: "recogaks-coturn"
    spec:
      containers:
        - name: "recogaks-coturn"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 3478